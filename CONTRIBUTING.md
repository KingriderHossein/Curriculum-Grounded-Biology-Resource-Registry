# راهنمای مشارکت

این پروژه یک نقشه‌راه علمیِ گزینش‌شده است، نه فهرست آزاد لینک‌ها.

## قبل از ایجاد Pull Request

ابتدا ISBN یا DOI را در `data/resources.json` جست‌وجو کنید.

- اگر منبع **از قبل وجود دارد**، آن را دوباره ثبت نکنید؛ فقط یک Placement جدید در `data/placements.json` بسازید.
- اگر منبع **جدید است**، هویت کتاب‌شناختی آن را یک بار در `data/resources.json` ثبت کنید و سپس Placement مربوط به درس را اضافه کنید.

## Resource چیست؟

Resource هویت جهانی کتاب، مقاله یا فصل است و شامل اطلاعاتی مانند این موارد می‌شود:

- عنوان؛
- نویسندگان؛
- ناشر یا ژورنال؛
- سال؛
- ویرایش؛
- ISBN یا DOI؛
- لینک رسمی؛
- لینک خرید کتاب در صورت وجود؛
- جلد کتاب.

این اطلاعات نباید برای هر درس تکرار شوند.

## Placement چیست؟

Placement مشخص می‌کند یک Resource در **یک درس مشخص** چگونه استفاده می‌شود:

- Subject ID؛
- مرحله یادگیری؛
- مفهوم‌های پوشش‌داده‌شده؛
- شاخه تخصصی، در صورت نیاز؛
- دلیل قرارگیری منبع در آن مرحله؛
- راهنمای فارسی دانشجو:
  - مناسب برای چه کسی؟
  - چرا این منبع؟
  - چگونه استفاده شود؟
  - قدم بعدی چیست؟

## معیار پذیرش

هر Placement باید پاسخ روشنی به این سؤال بدهد:

> چرا این منبع، برای این دانشجو، در این مرحله و در این شاخه قرار گرفته است؟

شهرت کتاب یا تعداد استناد مقاله به‌تنهایی کافی نیست.

## منابع مجاز

- کتاب نظری دانشگاهی؛
- کتاب مرجع؛
- مقاله مروری معتبر؛
- فصل نظری معتبر؛
- منبع دانشگاهی آزاد.

## خارج از دامنه

- پروتکل؛
- Laboratory Manual؛
- SOP؛
- آموزش wet-lab؛
- راهنمای کار با دستگاه؛
- آموزش عملی تکنیک‌ها.

## ساختار خلاصه هر درس در README اصلی

تمام درس‌های تکمیل‌شده در `README.md` باید **یک ساختار واحد** داشته باشند. الگوی مرجع، بخش بیوشیمی است:

1. عنوان درس با `##`؛
2. یک پاراگراف کوتاه برای توضیح مسیر؛
3. فهرست ۹ شاخه تخصصی؛
4. تیتر `### کتاب‌های اصلی مسیر`؛
5. یک جدول سه‌ستونه با:
   - منبع اصلی (`core`)؛
   - منبع تکمیلی (`intermediate`)؛
   - منبع پیشرفته (`advanced`)؛
6. برای هر کتاب: جلد، عنوان، نقش در مسیر، ویرایش/سال و لینک ناشر یا خرید؛
7. لینک «مشاهده نقشه‌راه» در پایان بخش.

نسخه خلاصه یک درس نباید فقط شامل عنوان، یک پاراگراف و لینک نقشه‌راه باشد. داده کتاب‌ها باید از `data/resources.json` و جایگاه آن‌ها از `data/placements.json` گرفته شود تا README با رجیستری اصلی ناسازگار نشود.

## اعتبارسنجی

پیش از ارسال PR اجرا کنید:

```bash
python scripts/validate_registry.py
```
